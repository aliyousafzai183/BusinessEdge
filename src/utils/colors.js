import { Dimensions } from "react-native"

const { width, height } = Dimensions.get('window');

const colors = {
    primary: '#EF463C',
    secondary: '#FAB246',
    background: '#2D2966',
    text: '#FFFFFF',
    white: '#FFFFFF',
    black: '#000000',
    height: height,
    width: width,
    
}

export default colors;